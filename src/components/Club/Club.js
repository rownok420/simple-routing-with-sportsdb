import React from 'react';
import MenuBar from '../MenuBar/MenuBar';

const Club = () => {
    return (
        <div className='bg-info'>
            <MenuBar />
            <h1>See all Club</h1>
        </div>
    );
};

export default Club;